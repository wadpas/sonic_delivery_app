import 'package:flutter/material.dart';

class StreamProvider extends StatelessWidget {
  const StreamProvider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
